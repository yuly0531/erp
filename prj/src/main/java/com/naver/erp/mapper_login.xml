<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.erp.LoginDAO">

   <select id="getLoginIdCnt" parameterType="hashmap" resultType="int">
		select count(*) from 
		  (select emp_id as id, emp_pwd as pwd from employee 
		  union all
		  select stu_id, stu_pwd from student
		  union all
		  select tea_id, tea_pwd from teacher
		  ) sub
		where sub.id=#{mid} and sub.pwd=#{pwd}
   </select>
</mapper>
